import 'package:flutter/material.dart';
import 'package:frontend/assets/views/main/settings.dart';

class GraphSettings extends StatelessWidget {
  const GraphSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Settings(),
    );
  }
}
