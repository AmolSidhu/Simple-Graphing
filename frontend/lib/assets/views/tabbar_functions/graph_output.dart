import 'package:flutter/material.dart';
import 'package:frontend/assets/views/main/output.dart';

class GraphOutput extends StatelessWidget {
  const GraphOutput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Output(),
    );
  }
}
